if (<%= @event.calendar_type %> == 1){
  $('.calendar').fullCalendar('removeEvents', [<%= @event.id %>])
  $('.modal').modal('hide');
}
else {
  $('.my_calendar').fullCalendar('removeEvents', [<%= @event.id %>])
  $('.modal').modal('hide');
}
